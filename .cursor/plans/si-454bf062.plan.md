<!-- 454bf062-50d7-496f-b10b-8cf08735fa76 65214657-ea7a-42b3-85e2-7b7ea71973bf -->
# Simple Testing Integration Plan

## Phase 1: Backend Cleanup

### 1.1 Create Archive Directory

Create `backend/@for future/` directory to store old implementations

### 1.2 Move Testing Files to Archive

Move ALL testing-related files from `backend/src/application/ai/testing/` (21 files):

- adaptive_test_executor.py
- api_test_agent.py
- autonomous_test_generator.py
- dependency_analyzer.py
- error_analyzer.py
- few_shot_examples.py
- intelligent_adaptive_executor.py
- intelligent_payload_generator.py
- intelligent_test_executor.py
- logistics_error_analyzer.py
- result_streamer.py
- smart_data_generator.py
- streaming_executor.py
- task_monitor.py
- test_coordinator.py
- test_data_generator.py
- test_executor.py
- vector_cleanup.py
- workflow_based_test_executor.py
- workflow_first_coordinator.py

### 1.3 Move Testing REST Endpoints to Archive

Move these files from `backend/src/presentation/rest/`:

- adaptive_testing.py
- autonomous_testing_realtime.py
- autonomous_testing.py
- realtime_testing.py
- testing_improved.py
- testing.py
- workflow_testing.py

Keep: partners.py, documentation.py, ocr.py, health.py, vision.py, chat.py, webhooks.py, vector_db.py

### 1.4 Clean Up Documentation

Move ALL markdown files to `backend/@for future/docs/` EXCEPT:

- README.md
- TEST_INSTRUCTIONS.md

Total: ~140 markdown files to archive

## Phase 2: Integrate Simple Testing Script

### 2.1 Convert Script to Module

Create `backend/src/application/ai/simple_testing/` directory with:

- `__init__.py`
- `flow_store.py` - FlowDataStore class (lines 27-130 from script)
- `document_extractor.py` - step1_extract_document (lines 132-173)
- `chunker.py` - step2_chunk_text (lines 176-195)
- `endpoint_analyzer.py` - step3_analyze_endpoints (lines 198-272)
- `payload_generator.py` - step5_generate_complete_payload (lines 308-377)
- `error_fixer.py` - step6_fix_payload_from_error (lines 380-452)
- `test_executor.py` - step7_test_endpoint_with_retry and step8_test_all_endpoints (lines 455-623)

### 2.2 Create New REST Endpoint

Create `backend/src/presentation/rest/simple_testing.py`:

```python
from fastapi import APIRouter, UploadFile, File, Form
from pydantic import BaseModel
from typing import Optional, List, Dict, Any

router = APIRouter(prefix="/api/simple-testing", tags=["Simple Testing"])

class TestRequest(BaseModel):
    partner_id: str
    documentation_id: str
    base_url: Optional[str] = None

class TestResult(BaseModel):
    endpoint: str
    status_code: int
    success: bool
    attempts: int
    final_payload: Dict[str, Any]
    response: Optional[Dict[str, Any]]
    error: Optional[str]

class TestResponse(BaseModel):
    status: str
    total_tests: int
    passed: int
    failed: int
    pass_rate: float
    results: List[TestResult]

@router.post("/test", response_model=TestResponse)
async def run_simple_test(request: TestRequest):
    # 1. Get document from MongoDB using documentation_id
    # 2. Extract text using document_extractor
    # 3. Chunk using chunker
    # 4. Analyze endpoints using endpoint_analyzer
    # 5. Run tests using test_executor
    # 6. Return results
```

### 2.3 Update Main App

Modify `backend/src/main.py`:

- Remove imports for old testing routers (lines 88-94, 103-109)
- Add: `from src.presentation.rest.simple_testing import router as simple_testing_router`
- Add: `app.include_router(simple_testing_router, tags=["Simple Testing"])`

## Phase 3: Frontend Integration

### 3.1 Simplify Testing API Client

Update `frontend/src/lib/api/testing.ts`:

- Replace complex TestingAPI class with SimpleTestingAPI
- New interface matching backend TestResponse
- Single method: `startTest(partnerId, docId, baseUrl)`
- Single polling method: `pollResults(testId)`

### 3.2 Update Onboarding Wizard

Modify `frontend/src/features/onboarding/components/steps/APITestingStep.tsx`:

- Remove complex progress tracking
- Call simple testing API
- Show simple progress (testing X of Y endpoints)
- Display pass/fail results

### 3.3 Remove Unused Testing Components

Move to `frontend/@for future/`:

- `src/features/testing/` (entire directory - complex terminal viewers, etc.)
- `src/lib/websocket/testSocket.ts`
- `src/types/testEvents.ts`
- Onboarding steps that use complex testing:
  - `AutonomousTestingStep.tsx`

### 3.4 Simplify IntegrationReviewStep

Update `frontend/src/features/onboarding/components/steps/IntegrationReviewStep.tsx`:

- Remove workflow display
- Focus on: endpoints found, base URL
- Simple "Start Testing" button

## Phase 4: Clean Up Dependencies

### 4.1 Update Requirements

Review `backend/requirements-simple.txt`:

- Ensure chromadb, mistralai, langchain-groq, httpx present
- Remove unused complex testing dependencies

### 4.2 Update Frontend Package

Review `frontend/package.json`:

- Remove websocket libraries if not used elsewhere
- Remove testing-specific dependencies

## Phase 5: Final Integration

### 5.1 Test Complete Flow

1. Start backend: `cd backend && python src/main.py`
2. Upload document via onboarding wizard
3. Verify simple testing endpoint works
4. Confirm results display correctly

### 5.2 Update Documentation

Update `backend/README.md` and `TEST_INSTRUCTIONS.md`:

- Document new simple testing flow
- Remove references to old complex system
- Add API endpoint documentation

## Files Modified Summary

Backend:

- Move: 21 testing files, 7 REST endpoints, ~140 markdown files
- Create: 7 new simple testing modules, 1 new REST endpoint
- Modify: main.py (remove old routers, add new one)

Frontend:

- Move: testing/ directory, websocket/, testEvents.ts, 1 onboarding step
- Modify: testing.ts (simplify API), APITestingStep.tsx, IntegrationReviewStep.tsx
- Remove: Complex progress tracking, terminal viewers, websocket connections

### To-dos

- [ ] Create @for future/ directory and move all old testing files and excess documentation
- [ ] Convert test_complete_flow.py into modular components under simple_testing/
- [ ] Create new simple_testing.py REST endpoint and integrate with FastAPI
- [ ] Simplify frontend testing API client to match new backend
- [ ] Update onboarding wizard to use simple testing flow, remove complex components
- [ ] Test complete flow from document upload to testing results